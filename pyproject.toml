# Generated By: Claude Code (Claude Opus 4.7)
# This repo is a Claude Code skill — SKILL.md plus helper scripts and JSON
# schemas. There is no installable Python package; pyproject.toml exists only
# to declare dev dependencies (pytest, jsonschema) used by the test suite.

[project]
name = "claude-skill-review"
version = "0.1.0"
description = "Claude Code skill: multi-agent codebase review with JSON-native pipeline"
readme = "README.md"
requires-python = ">=3.12"
license = {file = "LICENSE"}
authors = []
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "jsonschema>=4.0",
    "black>=23.0",
    "flake8>=6.0",
]
